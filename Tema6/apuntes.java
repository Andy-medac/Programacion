public class apuntes {

    public static void main(String[] args) {
        
        // char letra = 'h'; //un caracter

        String palabra = "hola"; // una cadena de caracteres

        //char arrayChar[] = {'h', 'o', 'l', 'a'}; Java hace internamente esto con los String

        System.out.println(palabra.length()); 

        // .indexOf() = muestra la primera posición donde aparece lo que introduzcas en el ()
        // .lastIndexOf() = muestra la última posición donde aparece lo que introduzcas en el ()
        // .charAt() = muestra el caracter que esté en la posición que introduzcas en el ()

    }
}
